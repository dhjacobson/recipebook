package com.example.recipebook.activities.my_recipes;

import android.view.View;

public class OnClickListenerCreateRecipe implements View.OnClickListener {
    @Override
    public void onClick(View view) {
        // create a new empty recipe
//        StupidDataAccessLayer dal = new StupidDataAccessLayer("10.0.2.2");
//        int recipeId = dal.createRecipe(new Recipe("Horse Soup"));

        // take us to the edit screen for the new recipe
//        MainActivity activity = (MainActivity) view.getContext();
//        activity.transitionToNewRecipe(recipeId);
    }
}
